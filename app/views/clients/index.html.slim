p#notice = notice

h1 Clientes

table.table
  thead
    tr
      th Nombre
      th Razon social
      th Reginmen
      th Actividad comercial
      th Dirección
      th Ciudad
      th Telefono
      th Email
      th colspan="2"

  tbody
  -@clients.each do |client|
    tr
      td = client.nombre
      td = client.razon_social
      td = client.regimen
      td = client.actividad_comercial
      td = client.direccion
      td = client.ciudad
      td = client.telefono
      td = client.email
      td = link_to 'Edit', edit_client_path(client), class: 'btn btn-primary'
      td = link_to 'Destroy', client, method: :delete, data: {confirm: 'Estas Seguro?'}, class: 'btn btn-danger'

br

= link_to 'Nuevo Cliente', new_client_path, class: 'btn btn-primary'
